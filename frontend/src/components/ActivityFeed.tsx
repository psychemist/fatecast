'use client';

// DISABLED FOR HACKATHON - was causing 60+ requests due to polling
export function ActivityFeed() {
  return (
    <div className="text-center py-8 text-gray-500">
      Activity feed temporarily disabled
    </div>
  );
}
